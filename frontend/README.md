# Vernier web

Next.js app for Vernier. See the repository root README for setup, API, and pipeline details.

```bash
npm install
npm run dev
```

`NEXT_PUBLIC_API_URL` defaults to `http://localhost:8000`.
